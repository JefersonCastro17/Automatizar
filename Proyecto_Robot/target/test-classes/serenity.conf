serenity {
    take.screenshots = FOR_EACH_ACTION
    console.colors = true
    logging = VERBOSE
}

webdriver {
    driver = chrome
    autodownload = true
    wait.for.timeout = 5000
    capabilities {
        "goog:chromeOptions" {
            args = ["start-maximized", "incognito"]
        }
    }
}